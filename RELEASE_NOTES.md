# v0.1.4

- Bumped `ato-benchmark-compare` dependency pin to `0.1.4`, bringing in updated small-business benchmark ranges and dataset comparisons.
- Synchronised `uv.lock` dependency metadata.

Not advice. Outputs are preparation aids for a qualified professional, not compliance determinations.
